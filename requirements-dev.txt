# Test-only dependencies (installed in the CI test stage, not in the image)
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23

# Doc generation — scripts/generate_flow_doc.py (Incident_Triage_Agent_Flow_v2.pdf)
reportlab>=4.0
